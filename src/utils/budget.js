// export const calculateTaxFormula =  (salary, taxRate) => salary * (taxRate / 100);

// export const wantsFormula = (salary, wantsBudget) => (salary - taxFormula)*(wantsBudget/100);
// export const needsFormula = (salary, needsBudget) => (salary - taxFormula)*(needsBudget/100);
// export const savingsFormula = (salary, savingsBudget) => (salary - taxFormula)*(savingsBudget/100);

// export const totalBudget = (wantsBudget, needsBudget, savingsBudget) => parseInt(wantsBudget) + parseInt(needsBudget) + parseInt(savingsBudget);